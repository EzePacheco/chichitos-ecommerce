export default function AdminPage() {
  return (
    <section className="placeholder-page">
      <div className="container">
        <div className="card placeholder-panel">
          <span className="eyebrow">Admin simple</span>
          <h1 className="section-title">Panel administrativo</h1>
          <p className="lead">Placeholder de ruta. No es seguridad: el acceso real debe validar Supabase Auth, rol admin server-side y bootstrap por allowlist.</p>
        </div>
      </div>
    </section>
  );
}
